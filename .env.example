# Copy to .env (gitignored). Loaded at startup; existing env vars win.

# Required when model.provider_url points at openrouter.ai (the default).
OPENROUTER_API_KEY=

# Required instead when provider_url is any other OpenAI-compatible endpoint
# (or set model.api_key_file in config.json).
# OPENAI_API_KEY=
